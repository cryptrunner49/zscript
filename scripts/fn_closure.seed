fn outer() {
    var a = 1;
    var b = 2;
    fn middle() {
        var c = 3;
        var d = 4;
        fn inner() {
            print a + c + b + d;  // Accesses variables from outer scopes
        }
        inner();
    }
    middle();
}
outer();  // Outputs: 10 (1 + 3 + 2 + 4)